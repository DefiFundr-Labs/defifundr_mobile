import 'package:flutter/material.dart';

class ReviewAndSignScreen extends StatelessWidget {
  const ReviewAndSignScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
